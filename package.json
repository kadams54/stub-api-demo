{
    "name": "stub-api-demo",
    "version": "0.0.0",
    "description": "Demonstrates options for stubbing out server-side API calls so that UI/client-side tests aren't disturbing/dependent on external systems.",
    "scripts": {
        "start": "node demo",
        "cypress:open": "cypress open"
    },
    "author": "Kyle Adams",
    "license": "UNLICENSED",
    "dependencies": {
        "nock": "^9.4.1"
    },
    "devDependencies": {
        "cypress": "^3.0.2"
    }
}
